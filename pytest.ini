[pytest]
testpaths = tests
# The default run is deterministic and offline: live RPC tests are excluded and
# must be requested explicitly with `pytest -m integration`.
addopts = -q -m "not integration"
markers =
    unit: fast, offline tests (no network)
    integration: tests that require a live Conflux eSpace RPC connection
